import { RegisterForm } from "@/components/auth/AuthForm";

export const metadata = {
  title: "Create account — ArthaSarthi",
};

export default function RegisterPage() {
  return <RegisterForm />;
}

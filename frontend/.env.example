# Server-side destination used by the same-origin /backend-api proxy.
BACKEND_API_URL=https://arthasarthi-backend-production.up.railway.app

# Optional: bypass the proxy and call a CORS-enabled backend directly.
# NEXT_PUBLIC_API_URL=https://api.example.com
